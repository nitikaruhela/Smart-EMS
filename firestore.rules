rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary debug rule while QR permission flow is being verified.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
